// Fichier généré automatiquement à la compilation.
// Cette version de secours est remplacée par le serveur.

struct SoundRef {
    let card: String
    let file: String
    let frames: [[UInt8]]
}

enum SoundRefs {
    static let all: [SoundRef] = []
}
